function checkValues(form) {
/*	var myregexp = new RegExp("^[_\.0-9a-z-]+@([0-9a-z][0-9a-z-]+\.)+[a-z]{2,6}$");
	if(!myregexp.test(form.email.value))
		alert("bad");
		return false;
	}*/

/*	for(var i = 0; i < form.childNodes.childNodes.length; i++) {
	if(form.childNodes[0].childNodes[i].tagName == "input") {
		if(form.childNodes.childNodes[0][i].value == "") {
			alert("fill in ALL the necessary fealds!");
			return false;
		}
		else
			alert(form.childNodes[0].childNodes[i].value + ": )" );
	}
	else alert(form.childNodes[0].childNodes[i].tagName);
	}
*/
//	return false;
}